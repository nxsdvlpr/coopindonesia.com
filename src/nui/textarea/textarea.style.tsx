import { tv } from 'tailwind-variants'

export const textareaStyle = tv({
  base: 'rounded-lg border border-gray-200 px-4 py-2.5 outline-none hover:border-primary-500 focus:border-primary-500 focus:outline-none',
})
